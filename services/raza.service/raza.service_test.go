package raza_service_test

/*
import (
	raza_service "gestion-de-mascotas/services/raza.service"
	"testing"
)

func TestSetRazas(t *testing.T) {

	filePath := "../../default-info/razas-mascotas.json"

	err := raza_service.Set(filePath)
	if err != nil {
		t.Errorf("Error al insertar las razas desde el archivo JSON: %v", err)
	} else {
		t.Logf("Razas insertadas correctamente desde el archivo JSON.")
	}
}

func TestGetRazas(t *testing.T) {
	tipoID := uint(1)

	razas, err := raza_service.Get(tipoID)
	if err != nil {
		t.Errorf("Error al obtener las razas desde la base de datos: %v", err)
	} else {
		t.Logf("Razas obtenidas correctamente para el tipo %d: %+v", tipoID, razas)
	}

	if len(razas) == 0 {
		t.Errorf("No se obtuvieron razas para el tipo %d desde la base de datos.", tipoID)
	}
}*/
